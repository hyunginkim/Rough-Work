#Importing Packages
import matplotlib.pyplot as plt
import random

#Creating Dice Roll Function
def roll_dice():
die_1 = random.randint(1,6)
die_2 = random.randint(1,6)

#Determining if die are the same outcome
if die_1 == die_2@
same_number = True
else:
same_number = False
return same_num

#Inputs
num_simulations = 10000
max_num_rolls = 1000
bet = 1

#Tracking
win_probability = []
end_balance = []
